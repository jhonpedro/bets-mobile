import styled from 'styled-components/native'
import getDimensions from '../../../utils/getDimensions'

const LowerTextContainer = styled.TouchableOpacity`
	margin-top: ${getDimensions(1.2).rem};
	margin-bottom: ${getDimensions(0.1).rem};
`

export default LowerTextContainer
