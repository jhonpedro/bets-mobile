import styled from 'styled-components/native'
import getDimensions from '../../../utils/getDimensions'

export const GameButtonListContainer = styled.ScrollView`
	height: ${getDimensions(2.2).rem};
	margin-top: 5px;
`
